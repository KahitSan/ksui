---
"@kahitsan/kplugin_transactions": minor
---

Author against the single `@kahitsan/plugin-sdk` package — import consolidation off the internal `@ks-erp/kernel-*` sources onto the one public SDK specifier. Behavior-preserving.
