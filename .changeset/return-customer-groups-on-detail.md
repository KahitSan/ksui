---
"@kahitsan/kplugin_transactions": patch
---

Fixed batch booking editing in the counter: the transaction detail endpoint now returns customer group data so the editor can properly render each customer's row. Also fixed the client pool name field to match what the counter UI expects.
