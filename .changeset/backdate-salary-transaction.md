---
"@kahitsan/kplugin_transactions": patch
---

createSalaryTransaction RPC now accepts backdate_reason and attachment payloads (base64). TransactionForm and related UI components now import PendingFile from @kahitsan/ksui instead of defining it locally.
