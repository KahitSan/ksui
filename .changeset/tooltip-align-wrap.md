---
"@kahitsan/ksui": patch
---

Tooltip: add optional `align` ("center" | "start") and `wrap` props so a bubble near a screen/sidebar edge can left-align and wrap instead of centering and overflowing under adjacent chrome.
