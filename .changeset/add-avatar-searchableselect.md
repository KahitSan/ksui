---
"@kahitsan/ksui": minor
---

Add generic `Avatar` (person/user avatar wrapping `AccountAvatar`) and `SearchableSelect` (single-select with client-side search wrapping `ComboBox`) components. Both are domain-free, letting plugins drop the kernel `@kserp/host-ui` Avatar/SearchableSelect wrappers.
