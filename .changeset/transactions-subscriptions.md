---
"@kahitsan/kplugin_transactions": minor
---

Powers the new Subscriptions page: lists each client's recurring plan (when it started, when it expires, how many renewals, lifetime value) and lets you renew a plan so the next billing window chains from where the current one ends.
