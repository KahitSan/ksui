---
"@kahitsan/kplugin_finance": patch
---

The counter board now serves through the availment projection even when the client omits the `limit` parameter, so older counter builds stay fast instead of falling back to the legacy recursive query.
