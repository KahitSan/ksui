// Decl shim so the docs tsconfig can type check the mock against the real
// contract. This references the canonical SDK decl that ships in the package;
// if the mock drifts from the package contract, the reference is the early
// warning.
/// <reference path="../../../host-ui.d.ts" />
